<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <groupId>com.mavensearch</groupId>
  <artifactId>mavensearch-parent</artifactId>
  <version>1.0.0-SNAPSHOT</version>
  <packaging>pom</packaging>

  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <tycho.version>5.0.4</tycho.version>
    <!-- 正式构建用官方源；本机可 -Declipse-p2.url=https://mirrors.tuna.tsinghua.edu.cn/eclipse/releases/2024-12/ 提速 -->
    <eclipse-p2.url>https://download.eclipse.org/releases/2024-12/</eclipse-p2.url>
    <!-- 契约运行 CLI 覆盖：-Dtests.excludedGroups= -Dgroups=contract（pom 硬编码值 -D 覆盖不了） -->
    <tests.excludedGroups>contract</tests.excludedGroups>
  </properties>

  <modules>
    <module>com.mavensearch.eclipse</module>
    <module>com.mavensearch.eclipse.feature</module>
    <module>com.mavensearch.eclipse.site</module>
    <module>com.mavensearch.eclipse.tests</module>
  </modules>

  <repositories>
    <repository>
      <id>eclipse-2024-12</id>
      <layout>p2</layout>
      <url>${eclipse-p2.url}</url>
    </repository>
  </repositories>

  <build>
    <plugins>
      <plugin>
        <groupId>org.eclipse.tycho</groupId>
        <artifactId>tycho-maven-plugin</artifactId>
        <version>${tycho.version}</version>
        <extensions>true</extensions>
      </plugin>
      <plugin>
        <groupId>org.eclipse.tycho</groupId>
        <artifactId>target-platform-configuration</artifactId>
        <version>${tycho.version}</version>
        <configuration>
          <executionEnvironment>JavaSE-17</executionEnvironment>
          <environments>
            <environment><os>linux</os><ws>gtk</ws><arch>x86_64</arch></environment>
            <environment><os>win32</os><ws>win32</ws><arch>x86_64</arch></environment>
            <environment><os>macosx</os><ws>cocoa</ws><arch>x86_64</arch></environment>
            <environment><os>macosx</os><ws>cocoa</ws><arch>aarch64</arch></environment>
          </environments>
        </configuration>
      </plugin>
    </plugins>
    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>org.eclipse.tycho</groupId>
          <artifactId>tycho-surefire-plugin</artifactId>
          <version>${tycho.version}</version>
          <configuration>
            <excludedGroups>${tests.excludedGroups}</excludedGroups>
          </configuration>
        </plugin>
      </plugins>
    </pluginManagement>
  </build>
</project>
